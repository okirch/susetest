
susetest parameter file handling
================================

This file contains the parameters to be passed to a test script. It
is usually generated on the fly when bringing up the SUT instances, and
is supposed to contain all relevant information on IP addresses, node
names and such.

Exactly how this is going to be done is still a bit in flux. More
to be added here when we've converged a bit on the data model.
